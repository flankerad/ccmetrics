"""ccmetrics — read-only token/cost meter for Claude Code JSONL transcripts.

Metadata only: counts, byte sizes, timestamps, tool names, paths, digests.
Never message text, file bodies, or tool-result bodies.
"""

__version__ = "0.1.0"
